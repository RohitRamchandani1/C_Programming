# Functions in `C`
- In `C` the executable statements are written inside a function.

- A `C` function is a collection of statements to perform a specific task.

- Every C program has at least one function called the main function.

- 